services:
  - type: web
    name: chatbot-api
    env: dotnet
    plan: free
    buildCommand: dotnet publish CahtBotApi/CahtBotApi.csproj -c Release -o ./publish
    startCommand: dotnet ./publish/CahtBotApi.dll